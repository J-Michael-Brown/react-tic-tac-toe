export default (state = {}, action) = {
  let newState;
  const { xIsNext, stepNumber, history, } = action;
  switch(action.type){
    case '':
      return state;
    default:
      return state;
  }
}
